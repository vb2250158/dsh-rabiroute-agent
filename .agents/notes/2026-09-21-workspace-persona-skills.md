# 按会话工作区发现人格技能

Rabi 持有工作区与技能；插件投影公开 Agent 状态，通过 DSH skills 服务接入。精确匹配不扩大到子目录，多个人格合并，身份摘要避免技能名称归一化冲突。

内存目录按可配置 TTL 失效，同时通知官方注册表清缓存。正文不缓存，加载核对会话工作区与 active 状态。发现请求共享注册生命周期，单个调用者取消不影响其他调用者。失败由官方注册表记录为未完成发现。

验证覆盖工作区隔离、路径归一化、重绑、错误恢复、并发、卸载及真实官方注册表、模型目录、技能工具和显式触发。远程实例接口要求管理认证，本实现仅使用公开 Agent 状态中提供的工作区。
