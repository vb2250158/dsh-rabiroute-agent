# 更新日志

[English](CHANGELOG_en.md) | 简体中文

## Unreleased

- 明确 Rabi 公共业务与 DSH 本地适配的职责，跨端合同供 Codex 等 Agent 复用。
- 增加不接入运行入口的纯消息封套解析源码与合成格式测试；保留原文、正文空白和失败回退，不执行回传 JSON。
- 记录聊天来源栏、原文查看及会话定位的未发布范围；界面接入优先评估公开插件替换与普通消息兼容，不改历史记录；尚未接入或部署。

## 0.2.0 — 2026-09-16

- 新增右侧栏「Rabi 计划」面板：会话绑定了 Rabi 计划时自动打开一次，会话标题旁的入口按钮可随时重开；面板只显示**绑定到本会话的那一个计划**，不显示目录、分页与记忆面板。
- 绑定判据取自 Rabi 自身规则 `plan.taskBinding.sessionId`（含计划秘书绑定），不新增 DSH 侧的第二套判定；会话绑定多个计划时按 Rabi 的语义只报告、不选择，并列出计划名。
- 面板不重画计划也不缓存计划：Host 侧新增同源只读路由 `GET /rabiroute/plan-panel?sessionId=…`，依次读会话绑定、分页读取该角色的当前计划摘要并本地匹配绑定、把角色映射到路由，最后返回 Rabi 的单计划地址；计划内容由 Rabi 自己的界面渲染。
- 各失败原因分别返回（`unbound` / `no-plan` / `multiple-plans` / `unrouted` / `no-session` / `unreachable`）；Manager 不可用时只报告原因，不用本地数据补造计划。
- 计划 tab 以本插件自己的 id 注册为独立页面类型，不接管任何既有 tab 类型；Manager 地址仍逐次发现并核对 `/meta` 身份。
- 与 Rabi 的接口面保持在既有只读接口内：不改 Manager 后端，因此只需前端 Web 热补丁（计划页新增 `/routes/:id/plan/:planId` 聚焦地址）即可配套上线；不读计划正文，也不拉整份目录。
- 验收范围：两端源码测试（本插件 46 项、Rabi WebGUI 302 项）与真实 Manager 只读探测通过；单计划视图的端到端渲染、插件安装、DSH 界面显示与跨机同步尚未验收。

## 0.1.5 — 2026-09-14

- 参考现行 Codex/Manager 合同，为 DSH 工具增加逐操作 Host 地址发现、健康和 generation/实例身份校验；移除固定端口默认值及提示词地址。
- 增加受限版本/幂等请求头、完整响应元数据、写后身份核验和明确的不确定结果；不自动重放业务请求。
- 开放只读健康与发送回执查询，加强路径、重定向、来源冲突和取消处理。
- 构建同步源码与真实包入口；补连接与失败路径回归，移除未加载的遗留 invariant 空模块。
- 已发布固定提交并通过官方入口的隔离安装，已安装包的动态 Host 发现与只读 `/meta` 验证通过。正式环境安装仍需按本机事务状态核对；独立 DSH Hook、业务投递及存储写入不属于本次只读运行验收。
