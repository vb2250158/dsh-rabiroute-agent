# 更新日志

## 0.10.2

- 内嵌计划地址携带当前 DSH 会话身份，供 Rabi 隐藏计划引导、自身关联项和空关联区域。

## 0.10.1

- 多计划执行门禁同时检查待归属附件；仅选择计划但遗漏附件时，仍要求逐组指定材料归属。

## 0.10.0

- 增加会话附件后台归档、多计划归属选择及步骤文件记录。保留待发送材料，使用强 ETag、幂等键和写后回读确认归档。

## 0.9.5

- 增加绑定计划上下文与详情查询参数；共享摘要缓存，支持多计划、过期提示及解绑清除。

[English](CHANGELOG_en.md) | 简体中文

## 0.9.4

修复冷启动标签被全目录查询超时清空：分页结果先显示，后续失败保留已有绑定，完整查询结束才移除失效绑定；后台总预算默认 120 秒，可配置。

## 0.9.3

多个绑定计划显示可切换目录；完整读取当前摘要，保留人格路由和 Rabi 状态配色。仅加载选中的详情页。

## 0.9.2 — 2026-09-21

- 接入 Manager 计划提交与状态配置事件，正常状态停止定时查询；密集事件合并刷新，重连校准，刷新期间保持颜色。

## 0.9.1 — 2026-09-21

- 状态名称和配色直接使用 Rabi 摘要接口的 presentation 配置。
- 刷新、过期与暂时失败不再改变标签颜色或文字；仅悬浮提示缓存状态，位置保持在标题后。

## 0.9.0 — 2026-09-21

- 增加侧栏会话计划状态标签；所有标签共用异步摘要缓存，避免逐会话扫描。
- 支持多绑定提示、缓存过期提示、后台超时与分页预算、隐藏页面暂停和卸载清理。
- 需要宿主通用会话行标签插槽；业务逻辑仍全部位于 Rabi 增强。

## 0.8.0 — 2026-09-21

- 按会话工作区发现 Rabi 人格技能，接入官方目录、模型加载和显式触发；缓存到期刷新绑定，隔离不同工作区。
- 修复仅有计划任务绑定、没有 Hook 人格记录时计划入口隐藏的问题；保留多绑定冲突与查询失败提示。
- 允许核心就绪的 degraded Manager 处理请求，健康诊断与身份核验分离；写后切代及无法核验仍标记不确定，禁止自动重放。
- 构建、技能提供器与真实 DSH 注册表集成测试通过，测试夹具使用匿名角色和工作区。

## 0.7.2 — 2026-09-20

- 麦克风改挂到官方自定义输入行最右侧，避开 `.field` 单格叠层，图标有描边和底色，可点。

## 0.7.0 — 2026-09-20

- 增加消息处理会话预设：仅允许 Rabi 接口，支持消息收发、Agent 会话与任务协调、计划管理；禁止命令与文件操作，并在工具执行层拒绝其他工具。
- 通用 Rabi 工具补充人格发现、人格间消息和回执接口；消息模式额外禁止附件读写。
- 明确专职协调职责，调查、方案和实施均委派；内置记忆、计划、人格 Skill 与协作接口指南，注入当前真实会话身份，并保留 Rabi 业务读取和记忆维护。

## 0.6.2 — 2026-09-20

- 语音入口改为自定义输入框最右侧的小麦克风图标，绝对定位且可点，不再被官方 textarea 挡住。

## 0.6.1 — 2026-09-20

- 提问卡包装改为等待官方 composer 注册后再挂上，避免加载顺序导致没有「自己的分支」和语音按钮。
- 自由文本出现后再附加按钮，只识别官方自定义输入的占位符。

## 0.6.0 — 2026-09-19

- 包住官方 `ask_user_question` 提问卡：自由文本标成「自己的分支」，并在该输入旁增加 Rabi 默认 ASR 语音按钮。
- 新增同源 `POST /rabiroute/speech/asr`，使用当前默认 ASR（本机为预加载 `faster-whisper/small`）转写浏览器 WAV，写回官方受控输入；不另选模型、不启动服务、不伪造选项。
- 测试覆盖默认模型、离线/缺省拒绝、非法音频、跨源请求和官方输入回填。页面录音与识别待安装验收。

## 0.5.0 — 2026-09-19

- 回复朗读升级为 24px 迷你播放器：生成中禁用点击并显示不定进度，完成后支持暂停、续播、拖动和时间显示。
- 使用 Rabi 默认 TTS 生成 WAV，由当前浏览器播放；退出消息释放音频，取消主机队列播放与旧回执提示。新增独立 speechTimeoutMs 生成预算；需重启 DSH 并刷新页面以同步前后端。

## 0.4.1 — 2026-09-19

- 根据真实接口验收修正默认 TTS 取值：`defaults.tts` 标识提供方，模型来自对应 `providers.tts` 记录，按 `提供方/模型` 传递。保留服务返回的具体拒绝原因。

## 0.4.0 — 2026-09-19

- 回复操作栏增加喇叭按钮，点击使用 Rabi 当前默认 TTS 朗读该条回复正文；服务未开启时提示原因，不自动启动。播放回执缺失时报告未确认，不自动重试。
- 新增同源语音适配和默认模型、离线、请求校验、重复点击、回执不确定性测试。正式安装与出声验收待完成。
- 语音入口允许必要能力就绪、身份一致且存活的 degraded Manager，随后单独检查 TTS 在线状态；其他工具和计划接口仍要求完整健康状态。

## 0.3.0 — 2026-09-17

- **非 DSH 来源的「定位到Agent」现在可用**：此前 `agentAdapter` 不是 `dsh` 时直接抛「暂不支持定位此外部 Agent 处理端」，于是 `codex` 来源虽然标题渲染成了按钮，点下去只会报错。现在 `codex` 等处理端交给 RabiRoute 唤起对应客户端窗口——本地 DSH 客户端根本没有那个会话，自己判断只会猜错。
- 新增同源路由 `POST /rabiroute/locate-agent`，请求体只有 `{ agentAdapter, threadId }`。Rabi 没有为 DSH 源站声明 CORS，浏览器不能直接调 Manager，所以由 Host 代答：核对 `/meta` 身份后调 `POST /api/agent/threads`（`action=open`）。只转发这两个字段，不代理消息正文与凭据。
- `dsh` 来源行为不变：仍在本地会话目录里按完整 ID 精确匹配后就地切换，不绕远路。`agentAdapter` 不在 Rabi 支持范围内时在发请求前就拒绝，Rabi 自己的拒绝原因则原样返回，不替它改写成更含糊的说法。
- 折叠行的定位提示文案更新，说明 DSH 直接切换、其它处理端交给 RabiRoute。
- 验收：源码测试 68 项通过（新增 `locate-agent` 13 项，含一条**用真实 HTTP 服务且不注入测试缝**的用例）。对运行中的 Manager 实测：截图里那个 codex 会话返回 `opened`、`owner=codex_desktop`、标题正确；伪造 ID、未支持处理端、缺 ID、非法请求体、错误方法各自返回精确原因。

## 0.2.2 — 2026-09-16

- **解析扩到「计划」与「系统」两类来源**：此前只认 `类型：Agent｜…`，其余一律回退原文平铺，于是系统事件（如 `agent_request_reminder`）整段铺在对话里。现在按来源类型各自的身份字段解析——计划取计划名称与计划 ID，系统取事件名称/类型/ID 及可选的触发方与消息路线。
- **计划与系统行默认折叠**：标题栏写它们描述的对象（`计划 · 名称（ID）` / `系统 · 事件名称 · 事件类型`），正文收在行内按钮后面；这两类没有可定位的会话，因此不出现「定位到Agent」——Agent 行保持原有直接显示与定位行为不变。
- 折叠时正文不渲染、也不跑 `projectUserText` 投影；展开与「查看原始消息内容」都能取回完整原文。
- 严格性不变并更明确：混用其他来源类型的字段、半写的来源身份、`消息端` 与任何未知拼写仍然回退原文。
- 验收：源码测试 53 项通过（信封 18 项、客户端 11 项）。运行实例显示待部署验收。

## 0.4.0 — 2026-09-19

- 明确 Rabi 公共业务与 DSH 本地适配的职责，跨端合同供 Codex 等 Agent 复用。
- 记录聊天来源栏、原文查看及会话定位的范围；界面接入优先评估公开插件替换与普通消息兼容，不改历史记录。

## 0.2.1 — 2026-09-16

- **修正入口的可见条件**：0.2.0 把按钮卡在"面板可用"（即计划已绑定）上，于是"会话已绑定人格、Rabi 还没记录计划"这个正常中间态里入口反而消失——正是需要它的时候。现在只要该会话有 Rabi 人格绑定（Host 路由返回非空 `roleId`）就显示入口；`roleId` 为空（未绑定或 Manager 不可达）时不显示。
- **入口改为 Rabi 图标按钮**：取自 `RabiRoute/assets/rabiroute-icon.png`，缩到 64px 内联进客户端产物（新增 `src/plan-icon.js`，约 4 KB），不运行时抓取——DSH 客户端没有可靠取得 Rabi 静态资源的地址，抓取还会让按钮闪空。图标-only 用按钮的 28×28 形态，标题与无障碍名沿用 `launcherHint`。
- **自动打开仍以"有计划"为条件**：已绑定但还没计划时不自动弹列，避免打开一个空面板；手动点击不受此限。
- 关闭行为不变且已在文档写明：关闭/折叠由 DSH 右侧栏提供，手动关闭后不会因切换会话再次自动弹出（自动打开按会话去重）。
- 验收：源码测试 46 项通过（新增入口图标与"已绑定无计划仍显示入口"两条断言）；DSH 下发产物已确认含入口注册与图标。运行实例显示待部署验收。

## 0.2.0 — 2026-09-16

- 新增右侧栏「Rabi 计划」面板：会话绑定了 Rabi 计划时自动打开一次，会话标题旁的入口按钮可随时重开；面板只显示**绑定到本会话的那一个计划**，不显示目录、分页与记忆面板。
- 绑定判据取自 Rabi 自身规则 `plan.taskBinding.sessionId`（含计划秘书绑定），不新增 DSH 侧的第二套判定；会话绑定多个计划时按 Rabi 的语义只报告、不选择，并列出计划名。
- 面板不重画计划也不缓存计划：Host 侧新增同源只读路由 `GET /rabiroute/plan-panel?sessionId=…`，依次读会话绑定、分页读取该角色的当前计划摘要并本地匹配绑定、把角色映射到路由，最后返回 Rabi 的单计划地址；计划内容由 Rabi 自己的界面渲染。
- 各失败原因分别返回（`unbound` / `no-plan` / `multiple-plans` / `unrouted` / `no-session` / `unreachable`）；Manager 不可用时只报告原因，不用本地数据补造计划。
- 计划 tab 以本插件自己的 id 注册为独立页面类型，不接管任何既有 tab 类型；Manager 地址仍逐次发现并核对 `/meta` 身份。
- 与 Rabi 的接口面保持在既有只读接口内：不改 Manager 后端，因此只需前端 Web 热补丁（计划页新增 `/routes/:id/plan/:planId` 聚焦地址）即可配套上线；不读计划正文，也不拉整份目录。
- 验收范围：两端源码测试（本插件 46 项、Rabi WebGUI 302 项）与真实 Manager 只读探测通过；单计划视图的端到端渲染、插件安装、DSH 界面显示与跨机同步尚未验收。

## 0.1.5 — 2026-09-14

- 参考现行 Codex/Manager 合同，为 DSH 工具增加逐操作 Host 地址发现、健康和 generation/实例身份校验；移除固定端口默认值及提示词地址。
- 增加受限版本/幂等请求头、完整响应元数据、写后身份核验和明确的不确定结果；不自动重放业务请求。
- 开放只读健康与发送回执查询，加强路径、重定向、来源冲突和取消处理。
- 构建同步源码与真实包入口；补连接与失败路径回归，移除未加载的遗留 invariant 空模块。
- 已发布固定提交并通过官方入口的隔离安装，已安装包的动态 Host 发现与只读 `/meta` 验证通过。正式环境安装仍需按本机事务状态核对；独立 DSH Hook、业务投递及存储写入不属于本次只读运行验收。

## 0.7.1 — 2026-09-20

- 增加提问卡自动语音滑轨开关；读取 Rabi 实时配置，复用官方输入和提交路径。
- 静音断句、自适应阈值、ASR 模型与自动提交跟随 Rabi；关闭或离开页面取消未完成提交。
