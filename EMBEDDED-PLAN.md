[English](EMBEDDED-PLAN_en.md) | 简体中文

# 会话内计划视图

计划面板地址携带 `embedAgent=dsh` 和当前会话的 `embedSession`。支持此协议的 Rabi Web 在内嵌页隐藏计划引导，并从关联 Agent 区域排除当前会话；其他绑定保留，空列表隐藏。独立打开 Rabi 页面仍保留完整计划操作。参数只控制展示，不替代计划权限或绑定校验。

切换回已打开过的会话时，插件复用已解析的绑定与页面地址，不再扫描计划目录。计划变更事件只让关联会话的解析失效；面板仍由 Rabi Web 渲染，Rabi 页面从浏览器会话存档立即展示详情，并以版本校验按需更新。
